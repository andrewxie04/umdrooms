{
  "name": "mapbox-web",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "npm run dev --prefix rooms-redesign",
    "start": "npm run dev --prefix rooms-redesign",
    "build": "npm run build --prefix rooms-redesign",
    "test": "npm test --prefix rooms-redesign",
    "bake:campus": "npm run bake:campus --prefix rooms-redesign",
    "update-data": "node scripts/update-buildings-data.js"
  },
  "//dependencies": "Runtime deps for netlify/functions/* ONLY (the frontend has its own package.json in rooms-redesign/). Netlify's function bundler resolves requires from the nearest package.json above the function file, i.e. this one — dropping cheerio here breaks dining-status and the libcal-booking-* functions at deploy time.",
  "dependencies": {
    "cheerio": "^1.2.0"
  }
}
